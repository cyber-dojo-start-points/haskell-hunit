module Checksum where

-- Nothing imports this module, but cyber-dojo.sh hands ghc every .hs file
-- under src and test, so a half written one is seen rather than sitting
-- there unnoticed.
checksum :: Int
checksum = 7 +
